{{ form_start(form) }}
            {% if form.vars.data.imageFilename|default %}
                <img src="{{ form.vars.data.imagePath|imagine_filter('my_large') }}" class="article-image-large">
            {% endif %}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
