{% extends 'layout.html.twig' %}
{% block title %}Admin{% endblock %}
    {% block sectionNav %}
    <section id="sectionNav">
        <div class="container">
            <h1>Admin</h1>
            <nav class="">
                <div class="" id="">
                    <ul class="">
                        {% if is_granted('ROLE_ADMIN') %}
                        <li class="">
                            <a class="" href="{{ path('user_index') }}">Users</a>
                        </li>
                        <li class="">
                            <a href="{{ path('hero_index') }}">CMS</a>
                        </li>
                        {% endif %}
                    </ul>
                </div>
            </nav>

    {% block adminContent %}
    {% endblock %}
        </div>
    </section>

    {% endblock %}