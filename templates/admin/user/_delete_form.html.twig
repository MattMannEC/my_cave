<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <label for="delete"><h5 for="delete" class="deleteLabel">Delete User ?</h5></label>
    <button class="btn btn-bordeaux" name="delete">Delete</button>
</form>
