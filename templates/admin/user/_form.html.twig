{{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.roles) }}
    {{ form_row(form.password, {
        attr : {
            value: '',
        }
    }) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.lastname) }}
{{ form_end(form) }}
